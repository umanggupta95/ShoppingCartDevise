<%= nested_form_for :product, url: products_path, :html => {:multipart => true} do |f| %>

  <%= render partial: "form", locals: {:f => f} %>

  <p>
    <%= f.submit :"Create Product" %>
  </p>
  
<% end %>
